[tool.ruff]
select = ["F", "E", "W", "I"] # F: Pyflakes, E/W: pycodestyle, I: isort
ignore = []
line-length = 88 
target-version = "py310"

[tool.ruff.per-file-ignores]
"__init__.py" = ["F401"] # Ignore unused imports int __init__

[tool.mypy]
files = ["src/"]
ignore_missing_imports = true